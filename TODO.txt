G R A P H 
=================================================
function getValueFromLabel(label){
    const regex = /label:\s*(.+)/
    const match = label.match(regex)

    if(match && match[1])
        return match[1].trim()

    return ''
}

- Add code that serializes the user's tree

A L G O R I T H M S
=================================================
1. Integrate Tree DS to following
   - Fibinocci
   - LCS

O T H E R 
=================================================
- Make a bridge class to get Qs and As from a 
  random implemented algo